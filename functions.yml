link-consumer:
  handler: bin/link-consumer
  events:
    - http:
      path: /link-consumer
      method: GET

save-images:
  handler: bin/save-images
  events:
    - http:
      path: /save-images
      method: POST
